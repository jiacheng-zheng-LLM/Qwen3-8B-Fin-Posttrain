# 推理链蒸馏说明

SFT 训练数据中，**题干、选项与标准答案是第三方真实金融考题；推理链（CoT）由本项目重新蒸馏**。

本项目照搬 DianJin-R1（[arXiv:2504.15716](https://arxiv.org/abs/2504.15716)）的蒸馏方法——
相同的题目集合、相同的数据量、相同的重试与校验流程——**唯一的改动是把生成推理链的教师模型
从论文使用的 DeepSeek-R1 换成 DeepSeek V4 Pro**。一致性校验沿用论文的 GPT-4o。

---

## 1. 题集构成

| 子集 | 论文 Table 1 | 本项目 | 说明 |
|---|---|---|---|
| CFLUE MCQ（中文金融选择） | 26 672 | **26 672** | 一致 |
| CFLUE 开放题 | 5 045 | **5 045** | 一致 |
| FinQA（英文数值推理） | 4 851 | **4 851** | 一致 |
| CCC（中文合规检查） | 1 800 | **未使用** | 论文的专有数据集，未开源 |
| **合计** | 38 368 | **36 568** | 差额即 CCC |

题目为 CFLUE（[aliyun/cflue](https://github.com/aliyun/cflue)）与 FinQA 的真实考题。
论文中 CFLUE 的 38 638 道原题经"长度 / 难度 / 歧义"三步过滤后得到上表数量，本项目沿用同一划分与规模。

> **本项目未使用上游 `DianJin-R1-Data` 数据集本身**，只沿用其字段格式、题集划分与数据规模；
> 其中的推理链由论文作者用 DeepSeek-R1 生成，与本项目无关。

---

## 2. 蒸馏配置

### 2.1 论文披露的部分（本项目沿用）

| 项 | 论文值 | 本项目 | 出处 |
|---|---|---|---|
| 生成推理链的教师 | DeepSeek-R1 | **DeepSeek-V4-Pro-0813**（正式版，2026-08-13 发布） | 论文 §数据构建 —— **本项目唯一的有意改动** |
| 每题最多尝试次数 | **T = 3** | T = 3 | 论文明确写出 |
| 失败样本处理 | 三次仍无法产出「答案正确且推理一致」的结果 → 保留为 non-reasoning 样本 | 同 | 论文明确写出 |
| 一致性校验模型 | GPT-4o | **GPT-4o** | 论文明确写出 |
| 校验的两个判据 | ① 预测答案 == 标准答案；② 生成的推理与参考解释一致 | 同 | 论文明确写出 |
| 输出格式 | `<think>…</think><answer>…\boxed{}…</answer>` | 同 | |

调用方式：**官方 API**（教师 DeepSeek-V4-Pro-0813，校验 GPT-4o）。
蒸馏日期：**2026-08-15**（与仓库内训练记录的时间线一致，见 §2.2）。

> 论文未报告蒸馏耗时与 API 成本。全文唯一涉及 API 调用次数的是 Table 6 的 8.15 次/实例，
> 那是多智能体合规系统的推理开销，与数据构建无关。这两项本项目同样无从对标。

### 2.2 论文未披露、本项目采用教师模型官方推荐值的部分

论文全文没有给出生成超参（已核对 arXiv HTML 全文的数据构建章节）。
本项目采用**教师模型 DeepSeek-V4-Pro-0813 的官方推荐值**：

| 项 | 值 | 出处 |
|---|---|---|
| **temperature** | **1.0** | 官方模型卡：「sampling parameters to `temperature = 1.0`」 |
| **top_p** | **1.0** | 官方模型卡：「`top_p = 0.95` for agentic scenarios and **`top_p = 1.0` otherwise**」——推理链蒸馏属非 agentic 场景，取 1.0 |
| **max output length** | **384K tokens** | 官方模型卡：「For the `high` and `max` reasoning effort levels, we recommend a maximum output length of **384K** tokens」 |
| `reasoning_effort` | `low` / `high` / `max` 三档 | 官方参数，控制作答前的思考量。上面 384K 的建议**仅适用于 `high` 与 `max` 两档**；若用 `low`，官方未给出对应的输出长度建议 |

来源：[DeepSeek-V4-Pro-0813 官方模型卡](https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro-0813)。

> **不要套用 DeepSeek-R1 的 0.6。** 论文教师是 R1（官方推荐 temperature 0.6 / top_p 0.95），
> 本项目教师是 V4-Pro-0813（官方推荐 1.0 / 1.0）——两代模型推荐值不同。
> V4 系列的公开指引还提到调低温度可能压垮推理链，建议用 `max_tokens` 控制长度而非降温。
> 换句话说，这一项**不该**与论文对齐，对齐反而是错的。

> **校验 prompt 原文未留存**（脚本遗失），§2.4 提供按论文两个判据复原的版本。

#### 时间线（与仓库内训练记录对账）

| 时间 | 事件 | 依据 |
|---|---|---|
| 2026-08-13 | DeepSeek-V4-Pro-0813 正式版发布 | 官方模型卡 |
| **2026-08-15** | **本项目蒸馏**（教师 V4-Pro-0813 + GPT-4o 校验） | — |
| 2026-08-17 23:16:57 | SFT 冒烟测试启动（已在消费蒸馏产出的数据） | ms-swift run 目录 `v0-20260817-231657` |
| 2026-08-18 00:26:59 | 正式 SFT 启动 | ms-swift run 目录 `v0-20260818-002659`；超参见 `weights_archive/sft-lora-checkpoint-1113-FINAL/args.json` |

08-15 落在 **08-13（模型发布）～ 08-17 23:16（数据已投入训练）** 的区间内，
与仓库内全部训练记录的时间戳自洽。

### 2.3 蒸馏脚本

原始脚本已遗失。仓库内提供一份**按论文方法复原的参考实现**：

- `phase1-sft-grpo/scripts/distill_cot.py`

> ⚠️ **这是复原件，不是产出本项目 SFT 数据的原始脚本。**
> 它依据论文公开描述的流程重写（T=3 重试、GPT-4o 双判据校验、失败样本降级为
> non-reasoning），供他人复现使用。用它重跑**不保证**得到与本项目完全相同的数据——
> 生成超参已知（§2.2），但校验 prompt 原文未留存，且 temperature=1.0 下教师本身带采样随机性。

### 2.4 一致性校验 prompt

原始 prompt 已遗失。`distill_cot.py` 内含一份**复原版**，按论文描述的两个判据构造：
① 预测答案是否等于标准答案；② 生成的推理是否与参考解释一致。

同样是复原件，措辞与原始 prompt 大概率不同。

---

## 3. 产物落盘与训练前处理

蒸馏产物按上游同名的字段结构与文件名落盘，因此归档 `args.json` 与实验日志中的
数据路径沿用了 `data/DianJin-R1-Data/train/` 这个目录名。
**该目录下的推理链是本项目产出，不是上游数据集的文件。**

训练前又做了两步处理（详见 `phase1-sft-grpo/EXPERIMENT_LOG.md`）：

1. **去污染** —— FinQA 与测试集重叠 37 条（0.76%）剔除 → `fin_qa.decontam.json`（4 814 条）
2. **长度过滤** —— 剔除 >5120 token 的样本 → 实训 **35 584** 条（保留 97.41%）

---

## 4. 外部模型调用的准确范围

| 环节 | 是否调用外部大模型 | 说明 |
|---|---|---|
| **推理链蒸馏** | ✅ 是 | 教师 DeepSeek V4 Pro + 校验 GPT-4o，照论文方法 |
| **评测判分** | ❌ 否 | FinQA 用 `fin_verify` 数值判分替代论文的 GPT-4o judge；CFLUE 用字母匹配。**因此 FinQA 分数不可与论文直接对标** |
| **难例筛选** | ❌ 否 | 以 SFT 模型自身采样通过率 `c/k ∈ (0,1)` 为唯一判据 |

关于难例筛选：论文用「教师重试 3 次仍失败 + GPT-4o 判推理一致性」来判难，
本项目改用客观通过率判据，**不是因为用不了 GPT-4o，而是设计选择**——
"教师做不出来"是难度的主观代理量，而 `0 < c/k < 1` 恰是 GRPO 组内优势非零的充要条件，
两者并不等价。实测 8000 题中全对 58.8% / 全错 11.0% 都落在零梯度区，
说明按论文口径随机抽 4096 会把近 7 成算力打在无梯度样本上。

---

## 5. 再分发说明

本仓库**不含**蒸馏产出的 SFT 数据（见 [DATA.md](DATA.md)）。

若后续决定发布该数据，需要**单独确认教师模型服务条款中关于「模型输出用于训练其他模型」的规定**
——这与题目本身的许可是相互独立的两件事：题目条款覆盖不了你用商业 API 生成的推理链，反之亦然。
